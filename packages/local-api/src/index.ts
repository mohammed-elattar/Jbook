export const serve = (port: number, fileName: string, dir: string) => {
    console.log('serving traffic from port ', port);
    console.log('saving/fetching cells from ', fileName);
    console.log('that file is in dir ', dir);
}